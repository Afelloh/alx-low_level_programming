#ifndef main.h
#define main.h

#include <stdio.h>

void positive_or_nmegative(int i);
int largest_number(int a, int b, int c);
void print_remaining_days(int month, int day, int year);
int convert_days(int month, int day);

#endif /* main.h */
