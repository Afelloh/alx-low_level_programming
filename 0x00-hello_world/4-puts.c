#include <stdio.h>

/**
 * main - prints out a string via puts
 * Description: Program that prints out the string s followed by a new line
 * Return: int 0 when done an no errors
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
